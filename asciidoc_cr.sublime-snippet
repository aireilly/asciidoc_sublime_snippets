<snippet>
	<content>. Create a `CUSTOM_RESOURCE` resource

.. Save the following YAML in the `FILENAME.yaml` file:
+
[source,yaml]
----
$0
----

.. Create the `CUSTOM_RESOURCE` CR:
+
[source,yaml]
----
\$ oc create -f FILENAME.yaml
----
</content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>cr</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>text.asciidoc</scope>
</snippet>